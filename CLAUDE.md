# 개발 지침서

## 공통 절대 규칙

1. **기존 비즈니스 로직 보존**: 지시받지 않은 모듈의 코드, API 인터페이스, DB 매핑 로직은 삭제하거나 함부로 수정하지 않는다.
2. **최소 변경 원칙**: 변경이 필요한 부분 위주로 최소한만 수정하여 diff를 줄이고 토큰을 절약한다.

---

## 프론트엔드 (Frontend Rules)

- **작업 범위**: `frontend/` 폴더 내 Tailwind CSS 스타일링 및 UI 구조 변경에 집중
- **디자인 스키마**:
  - 브랜드 색상: 오로라 키 컬러 (`#5cc8f1`, `#d6de9f`, `#eec7d3`, `#f7dca3`)
  - 카드/모달: `rounded-2xl`~`rounded-3xl`, `border border-white/60`, `bg-white/90 backdrop-blur-xl`
- **금지**: 촌스러운 진보라색 그라데이션, 과도한 3D 네온/이모지 남발 금지
